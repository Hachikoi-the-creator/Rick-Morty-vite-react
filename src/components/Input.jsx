// import { InputField } from "./styled/FormsStyles";

export default function Input(props) {
  const { holder, type, name } = props;

  return (
    <>
      {/* <label htmlFor={name}>Enter {name}:</label>
      <input type={type} name={name} placeholder={holder} /> */}
    </>
  );
}
